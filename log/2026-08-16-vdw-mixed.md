# 2026-08-16 — vdw-mixed

**Target.** Extend the exact frontier of the mixed two-color van der Waerden
table: decide `w(2;5,8)`, the next cell of the row whose last entry,
`w(2;5,7) = 260`, was computed by Tanbir Ahmed in 2013 (JIS 16, art. 13.4.4)
with a bespoke desktop backtracker and, as far as today's searching shows, has
not moved since. Secondary deliverable regardless of the frontier outcome: the
first certificate-backed derivations of the modern cells of the table —
`w(2;5,t)` for t = 5, 6, 7 and `w(2;4,t)` for t ≤ 8 — every UNSAT leg shipping
a DRUP proof checked by an independent from-definition verifier
(`tools/satcert/rup_check`), every witness re-verified by independent
enumeration. The 2013-era computations predate proof-logging practice; the
certificate gap is real and closable today.

Note: CLAUDE.md asks sessions to use the `conjecture-research` skill; it is
not available in this sandbox's skill list, so its checklist could not be
loaded. CLAUDE.md itself was followed directly.

## Connectivity check

- **WebFetch: fully blocked** (egress proxy whitelist). Failed today:
  arxiv.org, oeis.org, erdosproblems.com, mathoverflow.net, api.crossref.org,
  zbmath.org, boincsynergy.ca, web.iitd.ac.in, cs.utexas.edu,
  math.colgate.edu. No primary source was readable from this session.
- **WebSearch: working.** All literature claims below rest on search snippets
  retrieved 2026-08-16. **Every citation in this session is (secondary)** and
  is marked so. Claims of openness are as strong as today's snippets, no
  stronger.
- PyPI reachable (`python-sat 1.9.dev15` installed cleanly).

## Candidate slate (external)

**C1 — w(2;5,8), mixed van der Waerden numbers (Ramsey theory on the
integers).** Statement: least n such that every 2-coloring of [1,n] contains a
monochromatic 5-AP in color 0 or a monochromatic 8-AP in color 1. Sources
checked 2026-08-16 (all secondary, via snippets): Ahmed, "Some More Van der
Waerden Numbers", J. Integer Seq. 16 (2013) art. 13.4.4 — abstract reports
"the exact value of the previously unknown w(2;5,7)" = 260, with w(2;5,5)=178,
w(2;5,6)=206 the only prior values in the row; no snippet anywhere reports a
value or bound for w(2;5,8); Landman–Robertson's mixed-vdW line and
OEIS A217037 track the adjacent tables. Why believed open: the row's ledger
stops at 260 in every retrievable snippet, and the exact-value community
visibly moved to asymptotics after 2014 (Green, Forum Math. Pi 2022 — the
w(3,k) super-polynomial lower bound) and to certificates for Ramsey numbers
(the 2025 verified-certificates paper for R(3,8)/R(3,9), arXiv 2502.06055).
Caveat recorded: a BOINC project (boincsynergy.ca; Monroe's vdwnumbers line,
arXiv 1603.03301) grinds van der Waerden *lower bounds* by Rabung-style
colorings; its pages were unreadable today, so whether it has touched this
cell is unknown. Risk accepted and stated.

**C2 — open cells of Arora–Dwivedi–Tripathi, "Improved Two-Colour Rado
Numbers for Linear Equations with Certain Coefficients", EJC 32(4) #P4.63
(Dec 2025) (secondary).** The paper determines exact 2-color Rado numbers for
Σaᵢxᵢ = xₘ with 2-/3-distributable coefficient sets "and bounds otherwise" —
those bound-only cells are the freshest possible open-cells target, and the
instances are small (Rado scale, well inside this box). Not chosen **because
the paper is unreachable**: the list of open cells, and even the definition of
t-distributability, could not be pinned through snippets. Attacking "their
open cells" without being able to state which cells those are fails the
honesty bar. Revisit from a session with literature access.

**C3 — tightness of ART(5), the abelian repetition threshold over 5 letters
(combinatorics on words).** Samsonov–Shur conjectured their lower bounds on
ART(k) tight; Petrova–Shur ("Abelian Repetition Threshold Revisited",
arXiv 2109.09306) refuted tightness for k = 6..10 and left k = 5 as the case
still conjectured tight (secondary). Deciding it means proving avoidance at
the threshold — Dekking-style template machinery for abelian powers, an idea
bottleneck held by an active school, not a compute bottleneck. Scored out.

Subfields spanned: Ramsey theory on the integers, partition regularity of
linear equations, combinatorics on words.

Also examined and rejected during the survey: **w(2;3,20)** (conjectured 389
by Ahmed–Kullmann–Snevily, DAM 2014) — the natural headline target, killed by
feasibility: the t=19 predecessor consumed ~196 CPU-years on 200 Opterons in
2011 (secondary, snippet of the C&C literature); no 4-core session touches
t=20 honestly. **Erdős-problems database mining** — the 2026 AI wave (Quanta,
2026-08-03, secondary) is actively strip-mining precisely the
computationally-flavored entries; scooped-risk too high to verify through
snippets alone. **Weak Schur WS(6) ≥ 646** (Ageron et al. 2021, secondary) —
record-chasing against specialist local-search pipelines with 4 cores is a
coin flip with no certificate to show for a miss.

## Internal-thread assessment

Read the last five logs and the conjecture READMEs. Rotation: 08-12
signed-difference-sets, 08-13 distinct-subset-sums — no conjecture at two
consecutive sessions, nothing forced. Strongest live internal thread:
**distinct-subset-sums** — resume the certified `f(10)` sweep from 262 with
the multi-m deficiency-vector engine sketched in its NOTE §6.2 (projected
5–10× on node throughput). Honest sizing: a full session of engine work plus
sweep would move the frontier from 262 to roughly 275–285 of the 308 needed —
a ~10% range extension, exactly what the mandate's own standard says does not
change a row. The generalized-schur `(4,4,u)` ladder would change a row but
remains blocked on the DRUP-in-RAM toolchain defect recorded 08-13 (pysat
buffers proofs; no external solver binary installable). Verdict: no internal
thread beats the slate; default-external stands.

## Selection

**C1, w(2;5,8).** (a) The bottleneck is finite search with a known encoding —
two clause families over n Boolean variables — on hardware and solvers twelve
years past the frontier's last movement; the certification toolkit
(`tools/satcert`, built 08-05, validated but never yet used by a shipped
result) closes the proof gap the 2013 computations left open. (b)
Already-done check: the row's ledger (Ahmed's JIS paper, the Landman tables,
OEIS) stops at 260 in every snippet retrieved today; residual risk is the
unreadable BOINC project, stated above. (c) The result extends the
Landman–Robertson/Ahmed–Kullmann exact-values line — the tables every
Ramsey-theory-on-integers survey carries — and lands in OEIS's mixed-vdW
sequences; the certificates extend the 2025 verified-certificates practice
(arXiv 2502.06055) from Ramsey numbers to van der Waerden numbers.

What counts as achieving it: `w(2;5,8)` decided — a verified witness at w−1
plus a RUP-checked UNSAT proof at w. Graceful degradation, in order: (i) the
certified ladder t ≤ 7 (first proof-backed derivations of the row) plus a
resumable cube-and-conquer campaign at the (5,8) boundary with a committed
per-cube certificate manifest; (ii) an improved certified lower bound
`w(2;5,8) > n` from a verified witness at the largest n reached. Mid-session
checkpoint: if the calibration ladder prices the (5,8) UNSAT beyond a
resumable multi-session campaign (extrapolated growth against the measured
(4,6)→(4,7)→(4,8) and (5,5)→(5,6)→(5,7) curves), re-aim the UNSAT campaign at
the cheapest open cell the extrapolation admits and say so here.

## Result

**CERTIFIED.** Two headline items, plus infrastructure.

1. **`w(2;5,8) > 295`** — the first recorded bound on the open cell above
   Ahmed's 2013 frontier `w(2;5,7)=260`: an exactly-74-periodic good
   partition of `[1,295]` (no 5-AP in color 0, no 8-AP in color 1), found in
   0.2 s by a complete SAT search over periodic-with-≤8-defects colorings,
   verified by two independent from-definition checkers (Python and C
   re-enumeration). Walk history 280 → 290 → 292 → 295 committed.
2. **Seven cells of the mixed table carry machine-checkable proofs for the
   first time**: w(2;3,5)=22, w(2;3,6)=32, w(2;4,4)=35, w(2;4,5)=55,
   w(2;4,6)=73, **w(2;4,7)=109** (18,434,058-line DRUP proof, 1.14 GB,
   forward-RUP-checked in ~50 CPU-min), **w(2;5,5)=178** (1.56M lines). All
   agree with the published values (secondary), which shipped no
   certificates (1978–2013, pre-DRUP practice). Witness legs pass both
   verifiers; boundary certification rests on restriction monotonicity
   (NOTE Lemma 1).
3. **Structure of extremal witnesses mapped** (NOTE §4): exactly
   22-periodic at (4,5)@54; 44-periodic with exactly one defect at
   (5,5)@177 (complete search); **no** periodic witness with p ∈ [30,49] at
   (5,6)@205 (complete negative); exactly-74-periodic at (5,8)@295 with
   different blocks governing different n. Defect count grows along the
   ladder — this is what makes lower-bound legs computable in
   2^p·C(n,≤k) instead of 2^n.
4. Cube-and-conquer campaign driver validated on three controls (64/64
   leaves UNSAT-verified; SAT-cube detection; adaptive deepening closing
   421 leaves under forced budget; zero CHECKFAIL) with resumable
   append-only state — the vehicle for the multi-session UNSAT campaigns.

**In flight at close** (results land in the README table when their checks
complete; branch carries resumable state): w(2;5,6)=206 UNSAT leg (Glucose,
> 2 h — itself the calibration point: the row-5 proof-logged UNSAT cost
jumps ~×400 from (5,5) to (5,6)) and its witness leg; (5,7)@259 and
(5,8)@296+ witness probes.

## What failed

- **w(2;3,20) as target** — killed at the survey stage by feasibility
  arithmetic (t=19 cost ~196 CPU-years in 2011 (secondary)); logged so the
  next session doesn't re-price it.
- **Git swallowed 1.2 GB of proofs** and two commits timed out before the
  size policy (gitignore + MANIFEST) existed; a container restart during
  the hang killed two solver runs. Policy now in place from the first run.
- **CDCL on extremal witnesses**: CaDiCaL found the (5,5)@177 witness in
  0.24 s but hung 15+/35+ min at (5,6)@205 and (5,7)@259 — reproducing the
  folklore that satisfiable vdW instances defeat CDCL while local search
  and structure win.
- **Both local searches failed their positive controls**: sample-tabu
  stalled at cost 13 on known-SAT (5,7)@259; the violated-AP upgrade still
  could not close a *single* defect from a cost-1 seed at (5,5)@177
  (coordinated multi-flip basin). Replaced by complete (p,k)-restricted
  SAT, which is what actually worked.
- **Warm-start phase hints** (CaDiCaL `set_phases` from cost-12/49 seeds)
  closed nothing within minutes-scale budgets.
- **First periodic scanner had an honesty bug** — budget-exhausted periods
  (UNKNOWN) were not distinguished from definitive unsat in the final
  claim line; fixed before any completeness claim was recorded.
- **The (5,7)@259 witness remains unfound** (defect ansatz p ∈ [58,70],
  k ≤ 8 all UNKNOWN at 600k conflicts) — so the stretch goal, certifying
  w(2;5,7)=260, did not complete; its SAT leg is the sharpest open thread.

## Next

1. **Finish (5,6)**: the Glucose UNSAT@206 proof + a witness at 205
   (defect search at p=44, k ≤ 20 was still running at close) → eighth
   certified cell.
2. **(5,8) lower bound upward**: n=296+ needs either larger defect budgets,
   other periods (the complete per-period scan at n=300 was cut short), or
   two-block/interval ansatz; each verified witness is a permanent bound.
3. **(5,7)=260 certification**: witness at 259 via richer ansatz (defects
   k ≤ 20, longer budgets, periods beyond 70), then the UNSAT@260 campaign
   via cube-and-conquer (priced ≥ tens of Glucose-hours by the measured
   ×400-per-t growth; resumable CSV state is the point).
4. **Pin `w(2;4,9)`'s published status** from a session with literature
   access before touching that cell; same for the BOINC project's
   unpublished bounds.
5. Method thread worth a session: the merged-proof assembler
   (relativize per-cube DRUPs + case-split closure tree) to turn each
   completed campaign into ONE checkable proof file.
