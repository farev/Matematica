# 2026-08-10 — Erdős #699 (Erdős–Szekeres common prime factors of binomial coefficients)

**Target.** Erdős problem #699 [ErSz78]: is it true that for every
1 ≤ i < j ≤ n/2 there is a prime p ≥ i with p | gcd(C(n,i), C(n,j))?
Erdős–Szekeres strengthening: p > i outside a *finite* exceptional set of
triples (n,i,j). Status per both maintained mirrors, fetched today:
**open** (`teorth/erdosproblems` ground-truth YAML: `falsifiable`, status
last updated 2025-08-31; `google-deepmind/formal-conjectures` Lean: both
forms `research open`, statement formalized 2026-01-04). The only prior
verification found anywhere is an **unpublished forum post** (secondary,
author/date unrecoverable): a Rust sweep of the base form to n = 10⁷ plus
the families 2^k (k ≤ 27) and 3^m+1 (m ≤ 17), locating strict-form
failures at i=3, n = 3^m+1, j = n/2 for m ∈ {2,3,5,7,13} and the lone
i ≥ 4 exception (28,5,14). No published paper, no OEIS sequence
(DB: `oeis: N/A`), no recorded AI activity (wiki through 2026-06-30:
zero mentions of 699; neighbours 694/696/698 all resolved in 2026).

The specific result attempted today, stated before the production runs
(sweep launched 12:52 UTC, this file written while it runs):

1. **CERTIFIED** — a reproducible exact-arithmetic verification of the
   base conjecture and a **complete census of all strict-form (p > i)
   failures** for n ≤ 10⁹ (stretch 4·10⁹) — 100–400× the unpublished
   frontier, and the first verification with published code and
   dual-implementation controls at any range.
2. **PROVED** — structural lemmas: (L1) level i = 1 can never fail
   (via Kummer); (L2) the witness-window reduction i ≤ n − prevprime(n)
   that makes the sweep fast; (L3) an exact characterization of the
   i = 3 family: for n = 3^m + 1, the triple (n, 3, n/2) is a strict
   failure **iff** every prime p > 3 of 3^{2m} − 1 satisfies an explicit
   cofactor digit-domination condition, with the corollary that
   prime-power odd parts of 3^m ∓ 1 suffice — reducing the family to a
   twin repunit-primality question and thereby linking the finiteness of
   the ErSz exceptional set to standard prime heuristics.
3. **CERTIFIED** — decisions for the structured families at exponents far
   beyond any sweep: levels i ∈ {2,3} for n = 2^k and n = 3^m+1 as far
   as the factorizations of 2^k−1, 3^m∓1 cooperate (target k, m ≲ 120),
   hunting a **new exceptional triple beyond 2188** and, via L3 + certified
   primality, testing the repunit exponents m ∈ {71, 103, 541, 1091}.
4. The counterexample hunt rides along free: any strict failure at
   **composite i** (or prime i with i ∤ C(n,i) or i ∤ C(n,j)) refutes
   #699 itself; all 8 known failures are saved by p = i with i prime.

Success = (1) + (2-L3) with the census; stretch = a new exceptional
triple or a counterexample. Mid-session checkpoint 15:30 UTC: if the
sweep or L3 is stuck, descope to n ≤ 10⁸ + census + L1/L2 and log the
failure.

**Result.** *(to be completed at session close)*

**What failed.** *(to be completed at session close)*

**Next.** *(to be completed at session close)*

---

## 1. Connectivity check

| source | reachable | how |
|---|---|---|
| `arxiv.org` | **no** — EGRESS_BLOCKED at proxy | WebFetch |
| `oeis.org` | **no** — EGRESS_BLOCKED | WebFetch |
| `erdosproblems.com` | **no** — EGRESS_BLOCKED | WebFetch |
| `mathoverflow.net` | **no** | WebFetch |
| `renyi.hu` (Erdős paper archive), `escholarship.org` | **no** (probed today — the 1978 and 1988 primary PDFs are one hop away and unreachable) | curl + WebFetch |
| web search | **yes** — main literature channel; snippets carry verbatim statement text | WebSearch |
| `raw.githubusercontent.com` | **yes** — primary-source channel: `teorth/erdosproblems` `data/problems.yaml` (1217 problems, entry 699 fetched verbatim), `google-deepmind/formal-conjectures` `FormalConjectures/ErdosProblems/699.lean` (both open statements fetched verbatim), AI-contributions wiki (71.6 KB, full), `ehijano/rss_fetch` arXiv-RSS mirror (math.CO/NT/DS/GR, verbatim abstracts through 2026-08-08), `tczajka/octal-games`, `dmgordo/LJCR` | WebFetch/curl |
| `github.com` git protocol | **yes** — `git clone --depth 1` of `oeis/oeisdata` succeeded (~3 min): **full OEIS corpus (398,171 seq files, data through 2026-07-31) now local** — a new primary channel for this sandbox | git |
| `pypi.org` | yes (proxy bypass) | sympy 1.14, gmpy2 installed |

The `conjecture-research` skill named in CLAUDE.md is still not installed
in this sandbox (ListSkills: no match); its written discipline is followed
directly. Working branch is the environment-provisioned
`claude/kind-bohr-q0pfgr` (the harness forbids pushing elsewhere), not the
`claude/<conjecture>-<date>` naming the task prompt describes.
Hardware: 4 cores, 15 GB RAM, gcc 13.3.0, Python 3.11.15.

## 2. The three external candidates

Built from three parallel scout subagents (Erdős-#699 vetter;
fresh-arXiv quantity hunter over a validated RSS mirror, 5,140 abstracts
June–Aug 2026; diversifier with the OEIS corpus clone) plus my own
feasibility prototypes. Slate spans combinatorial number theory,
combinatorial game theory, and extremal combinatorics.

### E1 — Erdős #699, common prime factors of C(n,i), C(n,j) *(combinatorial number theory)* — SELECTED

*Statement.* As in the Target, pinned **verbatim** from the Lean
formalization (primary within that mirror): `erdos_699` (p ≥ i, all
1 ≤ i < j ≤ n/2) and `erdos_szekeres_strengthening` (p > i outside a
finite exceptional Finset), both `research open`. The classical
Sylvester–Schur single-coefficient lemma (some p > i divides C(n,i))
has a complete formal proof in `AllenGrahamHart/FormalConjectures-Bench`;
the gcd problem has none.

*Source.* `teorth/erdosproblems` YAML + Lean file, fetched 2026-08-10
(primary artifacts of the maintained mirrors); statement text of
erdosproblems.com/699 reconstructed from two agreeing search snippets
(secondary); [ErSz78] = Erdős–Szekeres, Austral. Math. Soc. Gaz. 5
(1978) 97–99, **unreadable from this sandbox** (secondary throughout);
Bergman, Bull. Aust. Math. Soc. 83 (2011) 138–157 resolves the
*neighbouring* gcd > 1 problem #698 (secondary).

*Why believed open.* Two maintained mirrors agree as of today; the AI
wiki (through 2026-06-30) has zero entries for 699 while its neighbours
fell in 2026; the only computation is the anonymous forum sweep
(secondary, treated as NUMERICAL-grade hearsay: no code, no author, no
certificate). Residual risk: [ErSz78] itself may contain small-case
data — its three pages are unreadable here; handled by marking every
statement about its content (secondary).

*Feasibility (measured before selection).* A 40-line Python prototype
reproduced the full known exception structure to n = 300 in 1.5 s and
cross-checked a big-int gcd path against the Kummer path (n ≤ 120,
exact agreement). A C brute force then swept n ≤ 3000 (4 cores,
~4 min): **8 strict failures, all saved by p = i** — (10,3,5),
(16,2,6), (28,3,14), (28,5,14), (244,3,122), (512,2,147),
**(2048,2,713) [beyond the forum's i=2 list]**, (2188,3,1094) — and the
fast engine (window factorization + prime-power CRT filter, measured
9.7 s per 10⁶ at n ≈ 10⁹ single-core) reproduces that census exactly.

*Who would cite it.* The erdosproblems.com page/forum for 699 (the
contribution channel its neighbours used); any eventual paper on the
ErSz strengthening; `formal-conjectures` (a certified census is
formalization-ready data).

### E2 — Octal game .404: is 286 the last P-position? *(combinatorial game theory)*

*Statement.* OEIS A394752 (entry created 2026-05-06): P-positions of
the octal game .404 are conjectured to be exactly {0,1,2,20,28,86,286};
"a(8), if it exists, exceeds 250000" (Irvine, 2026-07-12). Any new
Grundy zero refutes; a Guy–Smith period, if one exists, proves it.

*Source.* Local OEIS corpus clone, commit `4c8663620c` (primary);
`tczajka/octal-games` UnsolvedGames.md lists .404 unsolved with an empty
data row (primary); Flammenkamp's table **unreachable** (blocked).

*Why not selected.* The scout measured G(n) to 250,000 in 19 s (exact C,
reproducing the published P-list — strong candidate mechanics), and
10⁷–10⁸ is an afternoon. But the known-unknown is disqualifying today:
Flammenkamp's site is the community's record table for octal games and
is egress-blocked, so any "frontier extended" claim risks silently
rediscovering his range — the exact blocked-boundary failure mode
this log has been burned by before (08-04, 08-08/E2). Banked with the
measured feasibility for a day with Flammenkamp access.

### E3 — Sidon subsets of multinomial coefficients: extend Huber's s(n) table *(extremal combinatorics)*

*Statement.* arXiv:2608.02667 (Huber, 2026-08-05) computes, by SAT,
exact values s(n) = max size of a Sidon subset of the distinct
multinomial coefficients of order n, for n ≤ 16, and asks nothing less
than the growth rate; the table's next entries are open. Ground sets
grow slowly (|M(17)| = 193, |M(18)| = 243 — scout computed these
exactly), so n ≈ 25–30 is plausibly reachable with UNSAT certificates.

*Why not selected.* Freshest candidate of the day and no visible
competitors — but the paper's exact Sidon convention (repeats,
a+a sums) lives in a blocked PDF + blocked ancillary files, so the
publication boundary cannot be pinned primary today; and extending a
week-old single-author table is the shallowest of the three
mathematically. Banked; a session with the PDF readable makes it a
clean certified-table day.

### Also vetted and set aside today

Davenport constant d(H_343) window 18–24 (arXiv:2607.14379; another
AI research op has it formally "on reserve" with heuristic sweeps
already run — crowded); C(12,6,4) = 41 audit (arXiv:2607.23766; two
independent parties raced it within one week, both with full DRAT
pipelines); Mouhib Hales–Jewett/Gallai open cases (author self-scoops
within weeks, v2 already closed his own headline); Z(P(n,3)) (settled
by a third party one day after the paper); Gold-partition order 15
(cluster-scale); A030052 a(18) minimality (per-instance cost unbounded
a priori; Sawicki stopped at 17 in 2020 — gate on a calibration probe
first); A137295 octal .37 (same Flammenkamp caveat as E2); Heesch
records (CPU-months); union-closed n = 13 (not hours-scale). Meta-flag
for future sessions, recorded: at least five independent AI research
operations are actively mining the "small open case + certificate"
seam (txmy, dennisonbertram, Divide-By-0, przchojecki, jzone3);
certificate-shaped Erdős-database problems remain comparatively quiet.

## 3. The internal thread

Strongest live internal thread: **reciprocal-rado k = 9** — the 08-08
session left f₂(9) ∈ {245, 246} as a recorded prediction with the
n = 243 decide unfinished, and k = 10 is Conjecture B's first genuinely
new test; the CEGAR/DRUP toolchain exists but must be rebuilt in this
fresh container (~30 min per the 08-09 log). (a) compute-shaped: yes,
measured on 08-08. (b) novelty: certain. (c) cites Gaiser–Ramezanpour
directly. Runners-up: signed-difference-sets (32,20,4) family (measured
≈ 48 core-hours/group — not a session; the stronger-criteria thread is
ideas-shaped), generalized-schur (4,4,u ≥ 10) via disk-streaming DRUP
(feasible now, modest row change), Erdős–Gyárfás n = 19 via Carr's
constraints (generation measured ~30× the n = 18 scan; secondary-source
structural theorems).

The internal thread does not beat E1: it adds two values to an
already-certified table, versus first-published verification plus new
structural theorems on an *Erdős-database problem* whose openness was
pinned from primary mirrors this morning. Ties go to the new problem by
mandate; this is not a tie.

## 4. Selection argument

(a) *Compute-breakable?* E1: yes — measured at three scales before
selection (1.5 s prototype to 300; 4-minute brute census to 3000; 9.7 s
per 10⁶ engine at 10⁹). The structural lemmas L1–L3 were derived during
vetting, so the "ideas" half is already half-done. E2: yes (measured
19 s to 250k). E3: yes (estimated, convention-blocked). Internal: yes.
(b) *Already done?* E1: openness verified today in two maintained
primary artifacts; prior computation is unpublished hearsay at 1/100th
of today's target, with no census, no code, no certificates, and no
structural theory. E2: unknown Flammenkamp frontier (disqualifying).
E3: unknown convention (disqualifying for certified claims).
(c) *Citation target.* E1: the erdosproblems ecosystem (which
demonstrably absorbs contributions — 698 was resolved and recorded),
formal-conjectures, and any future ErSz-strengthening paper. E2: OEIS.
E3: one week-old paper.

E1 wins (b) and (c) outright and ties (a). The counterexample upside
(any composite-i failure refutes an Erdős problem) and the L3 link
between the exceptional set and repunit primes give it mathematical
depth the alternatives lack.

## 5. Tool discipline

Controls, all run before any production claim:
- **Dual implementation**: Python prototype (Kummer path) vs big-int
  gcd + trial factoring path, exact agreement n ≤ 120; C brute force
  (per-k prime bitsets, all pairs) vs fast engine (window factorization,
  prime-power CRT filter, per-level slow path): **byte-identical strict
  census on [4, 3000]** (8 EXC, 0 CEX). A third independent audit
  sampler (trial-division factoring, full j-scans at random (n,i))
  runs after the sweep; results in §6.
- **Positive controls**: the census reproduces the forum-reported
  structure exactly where ranges overlap (i=3 family {10, 28, 244, 2188},
  i=2 powers of 2 {16, 512}, the unique i ≥ 4 triple (28,5,14) — which
  the scout re-verified by exact arithmetic: gcd(C(28,5), C(28,14)) =
  1080 = 2³·3³·5).
- **Failure honesty**: engine v1 had a heap-overflow (primes array
  undersized — caught by a *crash*, fixed with an explicit cap assert);
  v2 briefly mis-flagged all primes ≤ √B as composite (caught by the
  [4,3001) census dropping to 4 entries against the brute-force 8) and
  had a completeness hole in the fast filter (small primes ≤ g used to
  kill candidates that higher levels may not see — caught by re-deriving
  the filter's necessity proof, fixed by restricting the filter to
  primes > g). All three incidents and their catches are in §7 and
  WRITEUP.md. The engine that ran production is v3.
- No randomness anywhere in the sweep; the audit sampler's PRNG seed is
  recorded. Runtimes, ranges, and per-worker stats in
  `conjectures/erdos-699/data/`.
