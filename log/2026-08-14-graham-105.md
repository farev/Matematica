# 2026-08-14 — graham-105

**Target.** Graham's $1000 problem (erdosproblems.com #376; Erdős–Graham–
Ruzsa–Straus, Math. Comp. 29 (1975) 83–92): are there infinitely many n with
gcd(C(2n,n), 105) = 1?  By Kummer's theorem the qualifying n are exactly those
with base-3 digits ≤ 1, base-5 digits ≤ 2, base-7 digits ≤ 3 (OEIS A030979).
The question is open and provably beyond any finite computation; what a
session can move is the problem's entire computational evidence base: the
known-terms census (published frontier: complete to 10^70, Thompson 2015),
the companion conjecture that n = 3160 is the last term also coprime to 11
(Graham, OEIS comment 2007), and the count data behind the N^0.02595
heuristic that is the reason Graham picked the infinite side. Plan: a
top-down search on leading base-3 digits with interval pruning in bases 5
and 7, aiming to push the complete census from 10^70 to 10^250 or beyond,
with the count-vs-heuristic table and the 1155 companion verified along the
way.

## Connectivity check

- **WebFetch: fully blocked**, exactly as on 08-13. arxiv.org, oeis.org,
  erdosproblems.com, mathoverflow.net, en.wikipedia.org, math.dartmouth.edu,
  mathstodon.xyz, garden.irmacs.sfu.ca and every other content domain tried
  by the vetting agents returned EGRESS_BLOCKED. Two exceptions:
  raw.githubusercontent.com and GitHub code search were reachable, which
  supplied verbatim mirrors of two OEIS entries (marked PRIMARY (mirror)
  below); one vetting agent also git-cloned a paper's data repository.
- **WebSearch: working.** All other literature claims are search-snippet
  evidence retrieved 2026-08-14 and are marked **(secondary)**. Claims of
  openness are as strong as today's snippets, no stronger.
- The `conjecture-research` skill is not present in this cloud environment's
  skill list; CLAUDE.md's written discipline was followed directly.

## Candidate slate (external)

**C1 — Graham's 105 problem, computational frontier (combinatorial/digital
number theory).** Statement: as in Target. Sources checked 2026-08-14:
erdosproblems.com #376 — open, $1000, "cannot be resolved with a finite
computation" (secondary); OEIS A030979 verbatim mirror at
raw.githubusercontent.com/africamonkey/OEIS-Spider (PRIMARY (mirror)):
Graham's $1000 comment, EGRS two-prime theorem, Pomerance's x^0.02595
heuristic, extension history (Nomoto 2002, Alekseyev 2008, Thompson 2015);
b-file line "Table of n, a(n) for n = 1..1374 (complete up to 10^70,
extends first 62 terms computed by Max Alekseyev)", Thompson, Nov 06 2015
(secondary, five independent snippets); Alekseyev's 2008 range reportedly
3^41 ≈ 3.65·10^19 (secondary, single snippet). Related work: Pomerance,
Amer. Math. Monthly 122 (2015) 636–644; Pomerance, Integers 26 (2026)
("Remarks on the middle binomial coefficient"); Croot–Mousavi–Schmidt,
Mathematika 70 (2024) e12249 (low p-adic valuations, not zero);
Bloom–Croot, arXiv:2509.02835 (Sept 2025) — "all but ε·log n digits small"
for sufficiently large bases; all (secondary). A DeepMind
formal-conjectures Lean stub for #376 is `sorry` (PRIMARY (mirror)).
Why open: the infinitude question needs ideas, not compute; the census
frontier (10^70) has no trace of extension since Nov 2015 — GitHub-wide
code search for the distinctive term 24991943715007537 finds only an OEIS
mirror; targeted searches for later extensions returned nothing.

**C2 — C4-free subgraphs of Q9 (extremal graph theory; erdosproblems #86).**
Statement: find ex(Q9, C4) — or at least beat the only known lower bound.
Sources: Minamoto, arXiv:2603.29127 (Mar–May 2026) — f(6) = 132 confirmed,
f(7) ≥ 304, f(8) ≥ 680, table stops at n = 8, open-problem list points at
n ≥ 9 (PRIMARY: the vetting agent cloned the paper's data repository,
verified SHA256 sums, and re-ran its dependency-free verifier on all
witnesses — 16 s, all pass); Brass–Harborth–Nienborg 1995 floor gives
f(9) ≥ 1498; no Q9 computation found anywhere (secondary). A vacant ledger
row: honest, session-feasible via simulated annealing + exact C4-freeness
certificate, with a deterministic fallback (Aut(Q7)-orbit count of the
19,866 optimal Q7 witnesses).

**C3 — Erdős's ternary powers of 2 (erdosproblems #406; digital number
theory/dynamics).** Statement: for n > 8, 2^n written in base 3 contains
the digit 2. Sources: erdosproblems #406 open, Erdős: "no method at our
disposal" (secondary); best density bound Narkiewicz 1980, ≤ 1.62·X^0.6309
(secondary); verification record n ≤ 2·3^45 ≈ 5.9·10^21 — Saye, J. Integer
Seq. 25 (2022) 22.3.4 = arXiv:2202.13256, settling both this and Sloane's
digit-0 conjecture in range (secondary). Why not chosen: the frontier is
four years fresh and a 10× push is weeks of compute, not hours; only an
audit-and-statistics session is honest here, and that is a weaker result
class than C1 or C2 today.

Also vetted and **disqualified**: Ankeny–Artin–Chowla — disproved by
Reinhart in Oct 2024 (counterexample p = 331,914,313,984,493,
arXiv:2410.21864; the p ≡ 3 (mod 4) Mordell analogue fell first,
Acta Arith. 215 (2024) 85–95, p = 39,028,039,587,479; treated as fact by
Fellini–Murty, J. Number Theory 285 (2026) 209–229) — all (secondary).
A "verify AAC further" session would have been work on a dead conjecture.
No-three-in-line was likewise vetted and dropped: solutions now known for
all n ≤ 70 plus 72 (Prellberg Sept 2025 for 47/49; Heule's SAT runs June
2026 for 65–70 and 72; Riley's GPU count of n = 19), leaving nothing a
4-core session could touch (all secondary).

Subfields spanned: combinatorial/digital number theory, extremal graph
theory, digit dynamics.

## Internal-thread assessment

Last two sessions: distinct-subset-sums (08-13), signed-difference-sets
(08-12) — no conjecture at two consecutive sessions, nothing rotation-
blocked. Strongest live internal thread: resume the `a(10)` sweep of
distinct-subset-sums (every cleared m is permanent; 08-13 log names the
multi-m deficiency-vector engine as the lever). But the row-changing event
there is `f(10)` decided, priced at CPU-months on this box; a session buys
`f(10) > ~270`, a non-row-changing increment. generalized-schur's (4,4,u)
ladder stays blocked on the SAT toolchain (pysat OOM at 15 GB; solver
sources unfetchable). signed-difference-sets' (32,20,4) family is priced at
~2 CPU-weeks. None beats the slate; default-external applies.

## Selection

Chose **C1** on the mandate's criteria. (a) Bottleneck: pure enumeration —
once the right algorithm is used. The terms have density ~N^0.026, so a
top-down DFS on leading base-3 digits, pruning whenever the almost-fixed
top base-5/base-7 digit strings already violate the caps, visits ~125
nodes per term found rather than the ~N^0.63 leaves of any bottom-up scan.
The whole published history of the problem fits in seconds of this
sandbox's time, and the marginal cost of each further order of magnitude
is ~6% more nodes. (b) Already done? The ledger is the OEIS b-file
(complete to 10^70, Nov 2015) plus Pomerance's papers; the vetting agent
specifically hunted for anything beyond 10^70 and found nothing.
(c) Extends: Thompson's census and Alekseyev's before it, the count data
behind Pomerance's heuristic (AMM 2015 §4, Integers 2026), Graham's 1155
finiteness remark, with results landing in OEIS A030979 and
erdosproblems #376's data notes. C2 was the runner-up and remains a good
future target; ties were to be broken toward the new, but C1 won outright
on (a) and (b).

What counts as achieving it: a certified complete census of A030979 to at
least 10^200 (vs 10^70), the count table it implies, and the 1155
companion pushed to the same height. Graceful degradation: the census to
whatever height the ladder completes, each rung a standalone certificate.

**Mid-session correction, logged as such.** The session's first two hours
were spent building and validating a *bottom-up* engine (Gray-code walk
over sums of distinct powers of 3 with staged mod-5^8/mod-7^6 filters,
32768 resume-safe tasks to (3^45−1)/2 ≈ 1.48·10^21), on the belief —
supported by the OEIS *display* line, whose last visible term is
≈ 2.5·10^16 — that the known frontier was 2.5·10^16. The literature agent
then established the b-file's true extent (10^70), which made that entire
campaign a replication of the bottom 10^-49th of the known range. The
campaign was stopped at a clean prefix (all n ≤ 3.66·10^19 > 3^41,
subsuming Alekseyev's entire reported 2008 range) and repurposed as the
independent bottom-range verification of the top-down engine that replaced
it. The cost of the wrong frontier assumption was ~2 h of wall time and
~90 CPU-minutes; the artifact it left is genuinely useful (a second,
algorithmically unrelated engine agreeing exactly on 62 terms and every
count to 3.66·10^19).

## Result

[TO BE FILLED FROM COMPLETED RUN DATA ONLY — the census ladder is still
executing as this section is drafted. Nothing in this section may be
written before the number it reports exists in a data file.]

Verification architecture: five implementations (brute force ≤ 2·10^7 with
literal gcd(C(2n,n),105) checks via math.comb; Python bottom-up DFS;
C bottom-up Gray-code grid; Python top-down; C top-down), pairwise
node-count- and fingerprint-identical on every overlapping range; C≡Python
top-down equality at L = 35/148/200/250/300 (nodes, terms, three
checksums, term lists); 4-worker task-mode reconciles with single-process
full-mode exactly at L = 300; every term printed by any engine passes an
independent digit-and-Lucas validator (Lucas route computes digits of 2n
and n separately, so it does not reuse the search's carry criterion);
external anchors: the 23-term OEIS display line, Thompson's 1374, and
Graham's 3160 all reproduced, never assumed.

## What failed

- **The bottom-up campaign** (see mid-session correction): right code,
  wrong target. Its 5.1-hour full plan would have "extended" a frontier
  that was actually 49 orders of magnitude further out. The failure was
  in step 2 of the survey — treating an OEIS display line as the extent of
  knowledge before the b-file was checked. Vetting agents ran concurrently
  with engine-building, which limited the damage to the cost of one
  refactor; serially, checking first would have cost less.
- **A self-matching pkill** killed the session's own shell mid-migration —
  the exact hygiene failure the 08-13 log warned about, repeated. The
  resume-safe task design absorbed it (zero recomputation lost beyond the
  killed workers' in-flight tasks).
- **Python recursion at L = 300** was launched as a background anchor and
  ran for 25+ minutes against the C engine's 18 s; killed after the L=250
  anchor had already given the Python≡C equality at scale. Python's role
  is validation, not production, and the session briefly forgot that.
- The first C engine draft had three real bugs (a wrong-stride cast that
  misindexed base-7 threshold comparisons, task prefixes mis-decoded when
  the split depth was not a multiple of 4, and no node-reconciliation
  path), all caught by the planned validation gates before any production
  run — none reached a certificate.

## Next

The census machinery is now trivially cheap to push (3^700 ≈ 10^334 costs
~2.5 h on this box; the marginal decade costs ~6% more nodes). The sharper
open threads: (1) the local-exponent swings — is there structure (a
log-periodic signature tied to the 3/5/7 digit alignment) that the
Fourier-side heuristic in Pomerance's §4 predicts quantitatively? A
session could compute the predicted oscillation and test it against the
G(3^k) table. (2) The 1155 companion is now far past any heuristic doubt
a computation can add; the interesting object is a *proof* that the
expected count converges — EGRS's two-prime method plus a fourth-base
large-sieve bound is the obvious first attack and its failure mode would
be informative. (3) OEIS: submit the extended b-file-style table
(terms to 3^200 committed here) and the corrected largest-term data to
A030979 once this repository's results are reviewed on a machine with
OEIS access.
