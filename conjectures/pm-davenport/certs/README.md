# Certificates

All files are exact-integer artifacts; each is verifiable with the tools
in the parent directory.

| file | content | verification |
|---|---|---|
| `c5c15_extremal_5sets.txt` | all 85,155 maximum dissociated 5-sets of `Z₅ ⊕ Z₁₅` as class-rep sets `(a,b)`, generated by Engine C (`refute_brute 5 5 15`, unpruned brute force) | `python3 verify_witness.py --file certs/c5c15_extremal_5sets.txt 5,15` (expect 85155 dissociated, 0 failed) |
| `c3p5p5_extremal_5sets.txt` | the same census computed independently in the CRT presentation `Z₃ ⊕ Z₅ ⊕ Z₅` by `orbit_analysis.py` (85,155 sets + 1 header line) | `python3 verify_witness.py --file certs/c3p5p5_extremal_5sets.txt 3,5,5` |
| `c3p7p7_extremal_7sets.txt` | all 2,016 maximum dissociated 7-sets of `Z₃ ⊕ Z₇ ⊕ Z₇` (≅ `Z₇ ⊕ Z₂₁`) — a single Aut-orbit (NOTE.md Theorem 11) | `python3 verify_witness.py --file certs/c3p7p7_extremal_7sets.txt 3,7,7` |
| `c5c15.count` | line count of the Engine C census (85,155) | `wc -l` |

The refutation side of Theorem 7 (no dissociated 6-set in `Z₅ ⊕ Z₁₅`)
is an exhaustive-search claim; its certificate is the reproducible run
(three engines, ≈ 7 s each; commands in ../README.md), not a file.
